apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: prometheus

resources:
  - deployment.yml
  - secrets.yml
  - volumes.yml
  - ingress.yml

configMagGenerator:
  - name: prometheus-config
    files:
      - config.yaml=prometheus.yml
